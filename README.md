# uni-ikura-song-dbtest

このリポジトリは静的な HTML ファイル (`index.html`) だけで構成されている簡易ビューアです。Node.js のビルドや依存関係管理を行っていないため、`package.json` は存在しません。

## テストについて
自動テストやビルド手順は用意していないため、`npm test` などの Node.js コマンドは実行できません。ブラウザで `index.html` を開いて動作確認してください。
